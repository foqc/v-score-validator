# Obsolete prompts

The historical prompt files that drove the browser-only programmatic rater have been removed.

Use instead:

* `/evaluate-idea` — Cursor command
* `.cursor/skills/` — evaluation and scoring skills
* `SPEC.md` / `ARCHITECTURE.md` / `AGENTS.md`
